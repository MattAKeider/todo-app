import { SelectStyleDirective } from './select-style.directive';

describe('SelectStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectStyleDirective();
    expect(directive).toBeTruthy();
  });
});
